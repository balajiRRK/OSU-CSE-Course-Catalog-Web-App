<div class="login-form-wrapper">
  <div class="login-form">
    <h2>New Grader Application</h2>

    <%= form_with(model: @grader, local: true) do |form| %>
      <% if @grader.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@grader.errors.count, "error") %> prohibited this grader application from being sent:</h2>

          <ul>
            <% @grader.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= form.label :name %>
        <%= form.text_field :name %>
      </div>

      <div class="field">
        <%= form.label :email %>
        <%= form.email_field :email %>
      </div>

      <div class="field">
        <%= form.label :phone_number %>
        <%= form.telephone_field :phone_number %>
      </div>

      <div class="field">
        <%= form.label :courses_wish_to_grade %>
        <%= form.text_area :courses_wish_to_grade %>
      </div>

      <div class="field">
        <%= form.label :courses_qualified_to_grade %>
        <%= form.text_area :courses_qualified_to_grade %>
      </div>

      <div class="actions">
        <%= form.submit "Send Application" %>
      </div>
      
      <div class="links">
        <%= link_to 'Back', graders_path %>
      </div>
    <% end %>
  </div>
</div>
